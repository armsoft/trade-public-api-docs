/documents/invoice:
  post:
    tags: [Invoice]
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/documents/Invoice.yaml
    responses:
      '201':
        description: Created
        content:
          application/json:
            schema:
              $ref: ../../components/schemas/documents/Invoice.yaml
      '401':
        $ref: ../../components/responses/Unauthorized.yaml
